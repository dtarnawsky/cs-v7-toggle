import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cs.v7.toggle',
  appName: 'cs-v7-toggle',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
